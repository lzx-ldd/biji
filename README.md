# biji
.push()在数组末尾加上元素
.unshift()在数组头部加上元素
.pop()抛出数组末尾的元素
.shift()抛出数组的第一个元素
console.log()打印出元素到控制台上
JSON.stringify(obj)是常用的将js对象转换为json字符串的方法
访问对象的两种方式一个是点操作符(.)，一个是中括号操作符([])。
git commit命令，-m后面输入的是本次提交的说明，可以输入任意内容，当然最好是有意义的
git commit命令执行成功后会告诉你，1 file changed：1个文件被改动（我们新添加的readme.txt文件）；
2 insertions：插入了两行内容（readme.txt有两行内容）
